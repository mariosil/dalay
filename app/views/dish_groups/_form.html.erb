<%= form_with(model: dish_group, local: true) do |form| %>
  <% if dish_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dish_group.errors.count, "error") %> prohibited this dish_group from being saved:</h2>

      <ul>
        <% dish_group.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
